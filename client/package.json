{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@testing-library/react": "^9.5.0",
    "@testing-library/user-event": "^7.2.1",
    "bootstrap": "^4.5.0",
    "materialize-css": "^1.0.0",
    "normalize.css": "^8.0.1",
    "react": "^16.13.1",
    "react-bootstrap": "^1.0.1",
    "react-device-detect": "^1.13.1",
    "react-dimensions": "^1.3.1",
    "react-dom": "^16.13.1",
    "react-intersection-observer": "^8.26.2",
    "react-router-dom": "^5.2.0",
    "react-scripts": "3.4.1",
    "rimraf": "^3.0.2",
    "sass": "^1.49.7",
    "web-vitals": "^0.2.3",
    "workbox-build": "^5.1.3"
  },
  "scripts": {
    "start": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts start",
    "test": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts test",
    "eject": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts eject",
    "build-sw": "node ./src/sw-build.js",
    "clean-cra-sw": "rimraf build/precache-manifest.*.js && rimraf build/service-worker.js",
    "build": "SET NODE_OPTIONS=--openssl-legacy-provider && react-scripts build && npm run build-sw && npm run clean-cra-sw"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "web-vitals": "^0.2.3",
    "workbox-build": "^5.1.3"
  }
}
